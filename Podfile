# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ipad_scrollapp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ipad_scrollapp
  pod 'SwiftFormat/CLI'

  target 'ipad_scrollappTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ipad_scrollappUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
